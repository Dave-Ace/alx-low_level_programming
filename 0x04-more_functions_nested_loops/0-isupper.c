#include "main.h"
/**
 *_issupper -- To check for upper case
 * @c: pArameter to check
 * Return: 0 or 1
 */
int _isupper(int c)
{
if ((_putchar(c) > 64) || (_putchar(c) < 91))
r = 1
else
r = 0
return (r);
}
