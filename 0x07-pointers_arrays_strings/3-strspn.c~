#include "main.h"
#include <string.h>
/**
 * unsigned int_strspn - gets length of a substring
 * @s: number of bytes in s
 * @accept: from
 * Return: unsigned int
 */
unsigned int_strspn(char *s, char *accept)
{
return (strspn(s, accept));
}
