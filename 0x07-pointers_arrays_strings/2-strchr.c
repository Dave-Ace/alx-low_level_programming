#include "main.h"
#include <string.h>
/**
 * *_strchr - locates a character in a string
 * @s: string
 * @c: occurence of c in s
 * Return: returns a pointer or null if not found
 */
char *_strchr(char *s, char c)
{
return (strchr(s, c));
}
