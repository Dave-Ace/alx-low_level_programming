#include "main.h"
#include <string.h>
/**
 * _strlen - to find the len of a string
 * @s: First parameter
 * Return: return the length
 */
int _strlen(char *s)
{
int len;
char st;
st = *s;
len = strlen(st);
return (len);
}
