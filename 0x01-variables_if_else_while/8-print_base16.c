#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int c;
int a;
for (c = 48; c < 58; c++)
putchar(c);
for (a = 97; a < 103; a++)
putchar(a);
return (0);
}
