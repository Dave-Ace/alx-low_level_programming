#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int c;
int d;
for (c = 97; c < 123; c++)
putchar(c);

for (d = 65; d < 91; d++)
putchar(d);

putchar(10);
return (0);
}
